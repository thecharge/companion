{
  "name": "@companion/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "echo 'server — no build step, runs directly'"
  },
  "dependencies": {
    "@companion/agents": "workspace:*",
    "@companion/config": "workspace:*",
    "@companion/core": "workspace:*",
    "@companion/db": "workspace:*",
    "@companion/llm": "workspace:*",
    "@companion/memory": "workspace:*",
    "@companion/skills": "workspace:*",
    "@companion/tools": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "1.1.42",
    "typescript": "6.0.2"
  }
}
